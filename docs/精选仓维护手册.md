# 精选仓 curated.json 维护手册

> 维护人：封哥 / 虾弟（Hermes）
> 最后更新：2026-08-11
> 仓库：https://github.com/jifeng250/tvbox-sources （GitHub 默认分支 main）

---

## 一、精选仓是什么

`curated.json` 是从全量仓 `tvbox.json`（1190 个源）里手工筛选出的 **17 个高质量影视源** + **7 个解析接口** 的精简配置，供 TVBox 使用。

**用户拉取地址（务必用这个 ghfast.top 代理前缀）：**

```
https://ghfast.top/https://raw.githubusercontent.com/jifeng250/tvbox-sources/main/curated.json
```

**spider（jar 加密壳，饭太硬版 fan.jar）：**

```
https://ghfast.top/https://raw.githubusercontent.com/jifeng250/tvbox-sources/main/fan.jar;md5;9d90c83bc7504392dd02c2236f08713a
```

> ⚠️ fan.jar 必须真实存在于仓库！曾有 fan.jar 404 导致加载隐患，已补传，MD5 如上。改仓后不要动 spider 字段。

---

## 二、源选取逻辑（三级闸门，缺一不可）

### 闸门 1：目录接口可拉取
- 调用 `{api}?ac=list`，必须返回 200 + 合法 JSON + `class` 数组（分类）
- 死链/超时/DNS 不通 → 直接排除

### 闸门 2：资源量足够大
- 每个分类下资源数要够大（合格线 ≈ 3~5 万+）
- 太小（几千部）的源没有存在价值，排除

### 闸门 3：播放链接必须是 m3u8 直链 + 分片可下载（★最重要）
- 取详情页 `ac=detail` → 检查 `vod_play_url` 是否返回 **m3u8 直链**
- 直链要能下载**首个分片**（实测下载成功 + 有一定字节量）
- 支持 master playlist 嵌套追踪（`#EXT-X-STREAM-INF` 里还有一层真实 m3u8）
- **网页链接源一律排除**——返回优酷/腾讯/爱奇艺官方网页、或本站播放页跳转的，TVBox 播不了（官采/建安/haiwaikan/快帆/量子的老毛病）

### 通过三闸门后还要看可用性
- 多线源优先（如玩家有多个播放线路）
- 长期稳定域名优先；接口经常换域名的源谨慎进仓

---

## 三、源类型说明（type 字段）

| type | 含义 | 精选仓现状 |
|---|---|---|
| 1 | CMS 直连（MacCMS 标准接口 `/api.php/provide/vod/`） | 暴風/索尼/闪电/新浪/光速/红牛/虎牙/极速/非凡/淘片/电影天堂/天涯/百度（13个） |
| 3 | CSP jar 爬虫（`csp_XXX`，依赖 spider 里指定的 jar） | 荐片/玩偶哥哥/小雅/热播（4个） |

**type=3 源关键点：**
- `api` 字段写 `csp_XXX`（不是 URL）
- 必须依赖仓库里的 fan.jar（饭太硬加密壳）才能跑
- `ext` 字段因源而异：
  - 玩偶哥哥：`{"Cloud-drive": "tvfan/Cloud-drive.txt", "from": "4k|auto", ...}`
  - 小雅：`{"vodPic": "https://up.mediy.cn/...", ...}`
  - 热播：`uqGL1bNENExT7/hGxpSE5qU=`（一段 base64，来自饭太硬版 qist 配置）

---

## 四、playerType 铁律（v8 事故教训）

**合法值只有：`0=系统播放器 1=ijk 2=exo 10=mxplayer -1=默认`**

> ❌ `playerType=3` 非法！曾因误写 3 导致**整个仓解析失败**（用户报"拉不到目录"），v8 回滚。

- type=1 的 CMS 源：不写 playerType（留空）或写合法值，推荐留空/0
- type=3 的 CSP 源：写 2（exo）通常最稳

---

## 五、筛选功能（filter）真相 —— 千万别再被坑

**结论先行：TVBox 对 type=1 CMS 源的筛选，只能来自接口本身**（`ac=list` 响应里的顶层 `filters` 字段，按分类 id 映射），**不是配置里能加的**。

- 实测 45+ 个 CMS 源（含 qist/多多/博客园接口），**全部不返回 filters** → 所以 CMS 源在 TVBox 里都没有筛选按钮
- 只有 **type=3 CSP 源**（如玩偶哥哥）由 jar 内置筛选，才有类型/年代/排序
- `filterable: 1` 只是开关，接口不返回 filters 就没用（加了无害）

**所以：别再试图给 CMS 源加筛选了，做不到。用户要用筛选就用玩偶哥哥。** 除非找到接口自带 filters 的采集站（目前没找到）。

---

## 六、已排除源（黑名单，别再加回来）

| 源 | 排除原因 |
|---|---|
| 官采（cj.jusj.top） | 返回爱奇艺官方网页，解析接口也救不了 |
| 建安资源 | 返回网页链接，与官采同病（虽 6.3 万资源） |
| haiwaikan | 拉取失败/不可播 |
| 快帆 | 拉取失败/不可播 |
| 量子资源 | 接口超时 |
| 多多系 33 源（土狗/波比/西柚/北雁/木子/MX 影院等） | 几乎全 DNS 不通 / 404 / 403 |
| lzizy1.com 镜像 | videolist 超时 |

---

## 七、精选仓源清单（17 源）

### 可直连播放（m3u8 直链实测通过）
- **百度**（4.8万，m3u8 直链 216KB/1.9s，2026-08-11 新加）
- **闪电资源**（m3u8 直链+分片可播）
- **虎牙**（m3u8 直链 957KB）
- **淘片 taopian**（m3u8 直链 992KB）
- **暴風**（m3u8 主文件 OK，中文 URL 被 Python 误报，TVBox 实际能播）
- **csp_SNzy 索尼**（m3u8 主文件 OK，偶尔超时）

### 依赖解析接口（播放页型，保留但标注"需解析"）
- 新浪/光速/红牛/极速/非凡/电影天堂 → 返回自家播放页，走 7 个顶层解析接口

### CSP 特色源（jar）
- **荐片**（csp_JPJGuard）
- **玩偶哥哥**（csp_WoGGGuard，4K 弹幕，**唯一有筛选**）
- **小雅**（csp_AList，云盘 4 驱动）
- **热播**（csp_AppTTGuard，饭太硬多线版，2026-08-11 新加）

### 7 个解析接口（parses）
虾米(jx.xmflv.com)/云解析/爱豆/看看/PM/咸鱼/m3u8 —— 虾米实测最快（0.7s）

---

## 八、更新步骤（发布新版本）

1. **本地改**：编辑 `curated.json`（工作目录：`C:\Users\tongtong\AppData\Roaming\reasonix\global-workspace\tvbox-sources\curated.json`）
2. **本地校验**：JSON 合法 + playerType 全是合法值 + spider 不动
   ```bash
   python -c "import json; d=json.load(open('curated.json',encoding='utf-8')); print('OK', len(d['sites']))"
   ```
3. **提交推送**（⚠️ 先 pull --rebase 再 push，远程可能有 CI 提交）：
   ```bash
   git add curated.json
   git commit -m "✨ vX 说明"
   git pull --rebase origin main && git push origin main
   ```
4. **GitHub 实拉验证**（确保能通过代理拉到）：
   ```bash
   curl -sL "https://ghfast.top/https://raw.githubusercontent.com/jifeng250/tvbox-sources/main/curated.json" -o /tmp/v.json && python -m json.tool /tmp/v.json > /dev/null && echo OK
   ```
5. 让封哥在 TVBox 重新拉取测试。

---

## 九、新增候选源的标准流程

1. 从全量仓/大配置（qist 0821、多多 xpg、博客园接口清单）收集候选 API
2. 跑"目录+资源量"筛选脚本（参考 `C:\Users\tongtong\tools\screen_new_cms.py`）
3. 跑"播放直链"验证脚本（参考 `C:\Users\tongtong\tools\verify_play_cands.py`、`verify_deep_m3u8.py`）
4. 三道闸门全过 → 加入 curated.json 并发布

---

## 十、常用脚本（都放在 C:\Users\tongtong\tools\）

| 脚本 | 用途 |
|---|---|
| screen_new_cms.py | 批量筛候选 CMS 源：目录 + 资源量 |
| verify_play_cands.py | 验证播放链接是否 m3u8 直链+分片可下 |
| verify_deep_m3u8.py | 深度 m3u8 验证（master playlist 嵌套追踪） |
| verify_curated_play.py / verify_retest.py | 精选仓全源播放实测 |
| scan_filter_sources.py | 扫描源接口是否自带 filters（筛选） |
| screen_duo_all.py | 多多配置全源批量测 |

---

## 十一、踩坑备忘录（血泪史）

1. **playerType=3** → 整仓解析失败（v8 事故），合法只有 0/1/2/10/-1
2. **网页链接源** → 永远播不了，别被"资源量大"骗（建安 6.3 万照样排除）
3. **Python 编码坑** → 暴風中文 URL 在 Python 触发 ascii 报错是**测试脚本**问题，TVBox 实际能播，别冤枉好源
4. **filterable=1 不等于有筛选** → 筛选数据只能来自接口 filters 字段，CMS 源普遍没有
5. **spider 字段别动** → fan.jar 必须真实存在，MD5 别改
6. **热播要用 csp_AppTTGuard**（饭太硬版），不是全量仓里的 csp_AppTT；ext 用 qist 配置里的 base64
7. **拉取一定要带 ghfast.top 前缀**，直连 raw.githubusercontent.com 在国内经常失败
8. **新源进仓前必测播放**，不能只看"能出目录"（目录=ac=list 有 class，播放=详情返回 m3u8 直链，两码事）